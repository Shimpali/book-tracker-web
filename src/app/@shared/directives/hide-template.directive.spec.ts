import { HideTemplateDirective } from './hide-template.directive';

describe('Directive: HideTemplate', () => {
  it('should create an instance', () => {
    const directive = new HideTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
